# Импорт основного приложения FastAPI
from app.main import app

# Проверка, что скрипт запущен напрямую (а не импортирован)
if __name__ == "__main__":
    # Импорт uvicorn для запуска ASGI сервера
    import uvicorn

    # Запуск приложения на хосте 0.0.0.0 (доступно извне) и порту 8000
    uvicorn.run(app, host="0.0.0.0", port=8000)
